apiVersion: v1
kind: Secret
metadata:
  name: agentsmith-lite-juicefs
  namespace: agentsmith
type: Opaque
stringData:
  name: agentsmith-lite-files
  metaurl: ${JUICEFS_META_URL}
  storage: s3
  bucket: ${JUICEFS_BUCKET}
  access-key: ${S3_ACCESS_KEY}
  secret-key: ${S3_SECRET_KEY}
